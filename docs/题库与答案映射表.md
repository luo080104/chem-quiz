# 题库与答案映射表（样机：Chapter 1a）

## 来源文件

| 角色 | 原始文件（保留，不覆盖） | 结构化副本 |
|---|---|---|
| 题目原件 | `data/source/Chapter1a_Atom_Questions.doc` | `data/source/Chapter1a_Atom_Questions.txt`、`data/raw/chapter1a.raw.json` |
| 候选答案（本次 AI 独立作答） | `data/answer-keys/chapter1a.deepseek-independent.json` | 合并进 `public/questions/chem-bank.json` |
| 手机 DeepSeek 答案（待核） | **尚未提供** | — |

## 每题字段（`public/questions/chem-bank.json`）

```jsonc
{
  "id": "CHEM-001",              // 稳定 ID，不复用旧 ID
  "version": 1,                   // 题库版本
  "originalNumber": 1,            // 原题号
  "chapter": "Chapter 1: Introduction of Chemistry and Matter",
  "tags": [],
  "type": "single",              // single | multiple
  "stem": "...",
  "options": [{ "key": "A", "text": "..." }],
  "answer": ["B"],               // 候选答案
  "candidateAnswer": ["B"],
  "explanation": "...",
  "reviewStatus": "pending",     // approved | pending | pending-image | contested
  "reviewFlag": null,
  "source": { "question": "原文件路径", "answer": "候选答案文件路径" }
}
```

## ID ↔ 原题号

- 题号 1–100 与 ID `CHEM-001`–`CHEM-100` 一一对应，序号连续无缺。
- 原题号 63 为图片选项题，图片已抽取渲染（`images/chapter1a/img002..006`），读图判读 `answer=["C"]`，`reviewStatus=pending`。
- 原题号 24 题干中的同位素符号为内联公式图（`img001`，WMF 已转 PNG）。
- 更正答案时：保留 `version` 递增与旧记录版本说明，**不覆盖原件、不复用旧 ID**。

## 校验与测试

| 项目 | 命令 | 结果 |
|---|---|---|
| 结构校验 | `npm run validate` | 100 题，**0 错误**，8 条待核警告（含图片选项、无答案题） |
| 逻辑测试 | `npm run test:logic` | 7 项全过（抽题去重/确定性、判分、错题历史、导入导出） |

报告：`data/reports/validate-report.json` 与 `.txt`。
